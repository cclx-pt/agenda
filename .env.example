# Credenciais da API inChurch (inRadar)
# Copie este ficheiro para `.env` e preencha com os valores reais.
# Estas variáveis são usadas apenas no servidor (proxy do Vite) e nunca
# chegam ao browser. Ver vite.config.js.

INCHURCH_API_KEY=your-api-key-here
INCHURCH_API_SECRET=your-api-secret-here
